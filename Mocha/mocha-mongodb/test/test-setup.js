// test-setup.js

// Load environment variables if needed (uncomment if using dotenv)
// require('dotenv').config();

// Connect to MongoDB
//require('./db').connectDB();

// Any other setup code you want to run before tests
console.log('Setting up test environment...');
